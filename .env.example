TELEGRAM_BOT_TOKEN=

# Dashboard (optional) — set DASHBOARD_ENABLED=1 to enable
# The backend has no built-in auth — keep DASHBOARD_PORT firewalled to localhost
# and access via SSH tunnel or a reverse proxy with HTTP basic auth in front.
DASHBOARD_ENABLED=
DASHBOARD_PORT=3110
